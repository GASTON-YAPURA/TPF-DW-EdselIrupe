# Conexión a PostgreSQL (obligatoria)
DATABASE_URL=postgresql://usuario:password@host:5432/edsellrupe

# Puerto del servidor (opcional, por defecto 3001)
PORT=3001

# Secreto para firmar tokens JWT (OBLIGATORIA - clave larga y aleatoria)
JWT_SECRET=cambia_esta_clave_por_una_larga_y_aleatoria

# Credenciales del panel admin (OBLIGATORIAS)
ADMIN_USER=admin
ADMIN_PASS=cambia_esta_contraseña_por_una_segura